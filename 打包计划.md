# 打包计划

## 目标

将 UI_app 打包为跨平台独立可执行文件，用户下载解压即可运行，无需安装 Python。

- **Windows**：`InductorCoil-UI-win64.zip`（含 `.exe`）
- **Linux**：`InductorCoil-UI-linux.tar.gz`（含可执行文件）
- **构建**：GitHub Actions 自动构建，推送 tag 触发，产物发布到 Release

## 技术选型

| 组件 | 选择 |
|------|------|
| 打包工具 | PyInstaller（最成熟，PS6/Qt 支持好） |
| 构建平台 | GitHub Actions（免费、跨平台 runner） |
| 依赖策略 | 全部打入包内（含 CUDA 版 torch） |
| 模型文件 | 不打入，用户自行放置 `.pt`，UI 内选择路径 |
| 分发格式 | Windows `.zip` / Linux `.tar.gz` |

## 需要新增的文件

```
UI_app/
  pyinstaller.spec              # PyInstaller 打包配置
  .github/
    workflows/
      build.yml                 # GitHub Actions 自动构建
```

## pyinstaller.spec 设计要点

```python
# 入口
a = Analysis(
    ['main.py'],
    pathex=[],
    binaries=[],
    datas=[
        ('core/*.py', 'core'),
        ('ui/*.py', 'ui'),
        ('configs/', 'configs'),
        ('LICENSE.txt', '.'),
        ('README.md', '.'),
    ],
    hiddenimports=[
        'ultralytics',
        'ultralytics.nn',       # YOLO 内部模块，动态加载
        'torch',
        'torchvision',
        'cv2',
        'numpy',
        'PIL',
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[
        'models',               # 模型文件由用户自行放置
        'data',                 # 录制输出目录
        '__pycache__',
        '*.pyc',
    ],
)

# 收集 ultralytics 内置 yaml 配置
from PyInstaller.utils.hooks import collect_data_files
a.datas += collect_data_files('ultralytics')

pyz = PYZ(a.pure)

# 单目录模式（需将 models/ data/ 放置在此目录下）
coll = COLLECT(
    a.binaries,
    a.zipfiles,
    a.datas,
    strip=False,
    upx=True,                   # UPX 压缩减小体积
    upx_exclude=[],
    name='InductorCoil-UI',
)
```

**注意事项**：
- `ultralytics` 内部有大量动态导入（`nn.modules`、`cfg` 等），需要完整收集 `hiddenimports`
- torch 的 C++ 扩展（`_C`、`caffe2` 等）PyInstaller 会自动收集
- PySide6 的 Qt 插件（`platforms/`、`imageformats/`）需完整打入
- UPX 压缩可减小 30~50% 体积，但构建时间增加

## GitHub Actions 工作流设计

```yaml
name: Build

on:
  push:
    tags: ['v*']           # 推送 v1.0.0 等 tag 时触发
  workflow_dispatch:       # 也可手动触发

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: pip install -r requirements.txt pyinstaller
      - run: pyinstaller pyinstaller.spec
      - uses: actions/upload-artifact@v4
        with:
          name: InductorCoil-UI-win64
          path: dist/InductorCoil-UI/

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: |
          sudo apt-get update
          sudo apt-get install -y libgl1-mesa-glx libglib2.0-0
      - run: pip install -r requirements.txt pyinstaller
      - run: pyinstaller pyinstaller.spec
      - uses: actions/upload-artifact@v4
        with:
          name: InductorCoil-UI-linux
          path: dist/InductorCoil-UI/

  release:
    needs: [build-windows, build-linux]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v3
      - run: |
          zip -r InductorCoil-UI-win64.zip InductorCoil-UI-win64/
          tar -czf InductorCoil-UI-linux.tar.gz InductorCoil-UI-linux/
      - uses: softprops/action-gh-release@v1
        with:
          files: |
            InductorCoil-UI-win64.zip
            InductorCoil-UI-linux.tar.gz
```

**Linux 额外步骤**：`apt-get install libgl1-mesa-glx libglib2.0-0` 解决 Qt 运行时缺少系统库的问题。

## 用户使用流程

1. 从 GitHub Release 下载对应平台包
2. 解压到任意目录
3. 将 `.pt` 模型文件放入 `models/` 目录（或任意位置）
4. 双击 `InductorCoil-UI.exe`（Win）或 `./InductorCoil-UI`（Linux）启动
5. 在 UI 中选择模型路径即可开始推理

## 每次发版流程

```bash
# 1. 修改代码并提交
git add -A && git commit -m "feat: ..."

# 2. 打 tag
git tag v1.0.0

# 3. 推送 tag（GitHub Actions 自动构建）
git push origin v1.0.0

# 4. 等待 ~15 分钟，Release 页面出现产物
```

## 预期产物大小

| 平台 | 未压缩 | 压缩后 |
|------|--------|--------|
| Windows | ~3.5 GB | ~2.0 GB（zip） |
| Linux | ~3.2 GB | ~1.8 GB（tar.gz） |

体积主要由 torch（~2.3GB）+ CUDA 库（~500MB）+ ultralytics/opencv（~300MB）组成。

## 已知风险与对策

| 风险 | 对策 |
|------|------|
| torch CUDA 版本与用户 GPU 驱动不兼容 | 构建时使用 CUDA 11.8 版 torch（兼容性最广） |
| 首次启动慢（PyInstaller 解压） | 使用 `--onedir` 模式，仅首次解压 |
| Linux 不同发行版 glibc 版本差异 | 在 `ubuntu-20.04` runner 上构建（glibc 2.31，兼容绝大多数发行版） |
| GitHub Actions 构建超时 | `timeout-minutes: 60`，分步构建 |
| ultralytics 更新导致内部 API 变更 | 锁死版本 `ultralytics==8.x.x` |
